{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -qE '\\.(tsx|jsx|html|css|vue|svelte)'; then node verifier/quick-lint.js \"$FILE_PATH\" 2>/dev/null || true; fi"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -qE 'git\\s+commit'; then cd verifier && npx playwright test --reporter=list --ignore-snapshots 2>&1 | tail -20; fi"
          }
        ]
      }
    ]
  }
}
